---
title: "Binary Search Variants: Lower ও Upper Bound"
description: শুধু আছে কিনা নয়, target-এর প্রথম বা শেষ জায়গা, কিংবা insert-পয়েন্ট খোঁজা। duplicate array-তে range আর count।
---

## কী শিখব

- target-এর প্রথম/শেষ অবস্থান, বা insert-পয়েন্ট খোঁজা

## ধারণা

অনেক সমস্যায় শুধু "আছে কিনা" জানলে হয় না। লাগে "প্রথমটা কোথায়", "শেষটা কোথায়", অথবা "নতুনটা বসলে কোথায় বসবে"। এখানেই দুটো দারুণ রূপ কাজে আসে:

- **lower bound:** এমন প্রথম index যেখানে মান target-এর সমান বা বড়
- **upper bound:** এমন প্রথম index যেখানে মান target-এর চেয়ে বড়

duplicate থাকা array-তে range বা count বের করতে এই দুটোই লাগে।

## কোড উদাহরণ

```js
// প্রথম index যেখানে arr[i] >= target
function lowerBound(arr, target) {
  let low = 0, high = arr.length; // high = length (exclusive)
  while (low < high) {
    const mid = Math.floor((low + high) / 2);
    if (arr[mid] < target) low = mid + 1;
    else high = mid;              // mid প্রার্থী হতে পারে, তাই রাখি
  }
  return low;
}
const a = [1, 2, 2, 2, 3, 5];
console.log(lowerBound(a, 2)); // 1 (প্রথম 2)
console.log(lowerBound(a, 4)); // 5 (4 বসত এখানে)

// একটি sorted array-তে target কতবার আছে
function countOccurrences(arr, target) {
  return lowerBound(arr, target + 1) - lowerBound(arr, target);
}
console.log(countOccurrences(a, 2)); // 3
```

## কোড ব্রেকডাউন

- `high = length` আর `high = mid` (mid + 1 নয়), এই ছোট্ট পার্থক্যই lower bound-কে ঠিক রাখে।
- upper বিয়োগ lower করলেই পেয়ে যাবে target কতবার আছে। পরিষ্কার কৌশল, তাই না?

## Complexity

- O(log n)।

## Animation Spec

> **Animation Spec: Boundary Finder**
>
> - **দৃশ্য:** duplicate-সহ sorted bar; lower আর upper সীমা দুই রঙে চিহ্নিত।
> - **ইনপুট:** array, target।
> - **ধাপ:** দুটো binary search চলবে; দুই সীমার মাঝখানটাই target-এর range।
> - **লক্ষ্য:** boundary দিয়ে ভাবা binary search-টা বোঝা।

## অনুশীলন

- `upperBound` নিজে লিখে ফেলো (`arr[mid] <= target` শর্তটা নিয়ে ভাবো)।
