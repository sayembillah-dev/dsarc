---
title: Data Structure কী? (What are Data Structures?)
description: লাইব্রেরির গল্প দিয়ে Data Structure-এর ধারণা, linear আর non-linear প্রকারভেদ, সাথে একই ডেটা ভিন্ন কাঠামোতে রাখলে কী হয়।
---

## কী শিখব

- Data Structure আসলে কী, আর কত রকমের হয়

## ধারণা

ভাবো তো, তুমি একটা লাইব্রেরিতে ঢুকলে।

- বইগুলো যদি এলোমেলো স্তূপে পড়ে থাকে, চাইলে বইটা খুঁজতে ঘণ্টার পর ঘণ্টা লাগবে।
- আর যদি বিষয় অনুযায়ী তাক-এ সাজানো থাকে, মিনিটেই হাতে পেয়ে যাবে।

ব্যাপারটা হলো, বই একই থাকে, শুধু সাজানোর নিয়মটা বদলায়। ডেটার বেলায়ও হুবহু একই কথা। একই ডেটা ভিন্নভাবে সাজালে কাজের গতি পুরো বদলে যায়। এই সাজানোর নিয়মটার নামই **Data Structure**: ডেটা এমনভাবে রাখা, যাতে পরে দ্রুত আর সহজে ব্যবহার করা যায়।

Data Structure মূলত দুই রকমের:

- **Linear:** একটার পর একটা সারিবদ্ধ, যেমন Array, Linked List, Stack, Queue।
- **Non-linear:** শাখা-প্রশাখা বা জালের মতো, যেমন Tree, Graph, Hash Table।

## কোড উদাহরণ

একই ডেটা, তিন কাঠামো:

```js
// একই ৩ জন ছাত্রের নাম, তিনভাবে রাখা:
const asArray  = ["Rahim", "Karim", "Jamal"];               // ক্রম আছে
const asObject = { s1: "Rahim", s2: "Karim", s3: "Jamal" }; // key দিয়ে
const asSet    = new Set(["Rahim", "Karim", "Jamal"]);      // ইউনিক মান
```

## কোড ব্রেকডাউন

- **Array:** ক্রম আর index দরকার হলে এটাই ভালো।
- **Object/Map:** নাম বা key দিয়ে সোজা খুঁজতে চাইলে এটাই।
- **Set:** শুধু ইউনিক মান রাখতে হলে এর জুড়ি নেই।

> **Animation Spec: One Data, Many Shapes**
>
> - **দৃশ্য:** একই কয়েকটা ডেটা এলিমেন্ট তিনটা ভিন্ন কাঠামোতে (list, tree, key-value) morph হয়।
> - **ইনপুট:** কাঠামো নির্বাচন dropdown।
> - **ধাপ:** নির্বাচন বদলালে একই এলিমেন্টগুলো নতুন বিন্যাসে সাজবে (smooth transition)।
> - **লক্ষ্য:** "ডেটা এক, কাঠামো অনেক", এই মূল ধারণা।

## অনুশীলন

- ভাবো তো, তোমার ফোনের contact list কোন কাঠামোতে সাজালে সবচেয়ে সুবিধা হবে? যুক্তিসহ বলো।
