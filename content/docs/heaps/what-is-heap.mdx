---
title: "Heap কী: Min-Heap ও Max-Heap-এর নিয়ম"
description: complete binary tree-এ parent-child নিয়ম; সবচেয়ে ছোট/বড় মান সবসময় root-এ।
---

## কী শিখব

- Min-heap ও Max-heap-এর নিয়ম

## ধারণা

Heap একটা **complete binary tree** (উপর থেকে বাঁ থেকে ডান পূর্ণ) যা একটা নিয়ম মানে:

- **Min-Heap:** প্রতিটা parent তার চাইল্ডদের চেয়ে **ছোট/সমান**, তাই সবচেয়ে ছোট মান root-এ।
- **Max-Heap:** প্রতিটা parent তার চাইল্ডদের চেয়ে **বড়/সমান**, তাই সবচেয়ে বড় মান root-এ।

```
Min-Heap:      1
              / \
             3   6
            / \
           5   9
```

<Callout type="info" title="মনে রাখো: heap পুরোপুরি sorted নয়">
heap শুধু parent-child সম্পর্ক মানে; তাই সবচেয়ে বড়/ছোট দ্রুত পাওয়া যায়, কিন্তু বাকি ক্রম নিশ্চিত নয়।
</Callout>

## কোড উদাহরণ

```js
// root সবসময় সবচেয়ে ছোট (min-heap): peek() O(1)
// insert/extract O(log n)
const minHeap = [1, 3, 6, 5, 9]; // array-রূপ, বিস্তারিত পরের পাতায়
const peek = (heap) => heap[0];
console.log(peek(minHeap)); // 1, সবচেয়ে ছোট মান মুহূর্তে
```

## কোড ব্রেকডাউন

- BST-তে বাঁ, root, ডান-এর ক্রম-নিয়ম; heap-এ শুধু parent vs child, নিয়ম ভিন্ন।
- BST দিয়ে min খুঁজতে O(log n); heap-এ min সবসময় root-এ, তাই peek O(1)।

## Complexity

- peek: O(1); insert/extract: O(log n)।

## Animation Spec

> **Animation Spec: Heap Property Check**
>
> - **দৃশ্য:** tree; প্রতিটা parent-child জোড়ায় নিয়ম মানছে কিনা সবুজ/লাল টিক।
> - **ইনপুট:** heap type (min/max) toggle।
> - **ধাপ:** নিয়ম-ভঙ্গ জোড়া লাল হবে; শিক্ষার্থী নিয়মটা বোঝে।
> - **লক্ষ্য:** heap নিয়ম বনাম BST নিয়ম আলাদা করা।

## অনুশীলন

- একটা array heap-নিয়ম মানছে কিনা হাতে যাচাই করো।
