---
title: "MinHeap ক্লাস: সম্পূর্ণ ইমপ্লিমেন্টেশন"
description: bubble up আর bubble down মিলিয়ে একটা পুরো reusable ক্লাস; push/pop O(log n), peek O(1)।
---

## কী শিখব

- একবার লিখে বারবার ব্যবহার করা যায় এমন একটা heap class

## ধারণা

চলো এবার আগের দুই পাতার জিনিস দুটো, মানে insert-এর bubble up আর extractMin-এর bubble down, একটা class-এ গুছিয়ে ফেলি। ব্যাপারটা হলো, আলাদা আলাদা function ছড়িয়ে না রেখে একটা পুরো **MinHeap** বানিয়ে নিলে যেকোনো জায়গায় যতবার খুশি ব্যবহার করা যাবে। ভেতরে থাকবে একটা array, আর বাইরে থেকে তুমি শুধু `push`, `pop`, `peek` ডাকবে।

## কোড উদাহরণ

```js
class MinHeap {
  constructor() { this.data = []; }
  size()  { return this.data.length; }
  peek()  { return this.data[0]; }          // min, O(1)

  push(value) {
    this.data.push(value);
    let i = this.data.length - 1;
    while (i > 0) {
      const p = (i - 1) >> 1;               // parent (fast /2)
      if (this.data[p] <= this.data[i]) break;
      [this.data[p], this.data[i]] = [this.data[i], this.data[p]];
      i = p;
    }
  }

  pop() {
    const n = this.data.length;
    if (n === 0) return undefined;
    const top = this.data[0];
    const last = this.data.pop();
    if (this.data.length) {
      this.data[0] = last;
      let i = 0, m = this.data.length;
      while (true) {
        let s = i, l = 2*i+1, r = 2*i+2;
        if (l < m && this.data[l] < this.data[s]) s = l;
        if (r < m && this.data[r] < this.data[s]) s = r;
        if (s === i) break;
        [this.data[i], this.data[s]] = [this.data[s], this.data[i]];
        i = s;
      }
    }
    return top;
  }
}

const mh = new MinHeap();
[5, 3, 8, 1, 2].forEach(v => mh.push(v));
console.log(mh.pop(), mh.pop(), mh.pop()); // 1 2 3
```

## কোড ব্রেকডাউন

- `push` মানেই তো insert, ভেতরে আগের সেই bubble up-ই ঘটে। আর `pop` মানে extract-min, সেখানে ঘটে bubble down।
- একটা জিনিস লক্ষ করো: `(i-1) >> 1` লেখা হয়েছে। এটা আসলে `Math.floor((i-1)/2)`-এরই কাজ, শুধু bit operation দিয়ে, তাই একটু দ্রুত।
- Max-heap দরকার হলে? সব তুলনার `<` গুলোকে `>` বানিয়ে দিলেই হলো।

## Complexity

- push আর pop দুটোই O(log n)। peek শুধু root-এ তাকানো, O(1)।

## Animation Spec

> **Animation Spec: Full Heap Ops**
>
> - **দৃশ্য:** array আর tree পাশাপাশি সিঙ্কে; push আর pop বাটন।
> - **ইনপুট:** মান, আর heap type।
> - **ধাপ:** push দিলে bubble up, pop দিলে bubble down; প্রতিটা swap গোনা হয়।
> - **লক্ষ্য:** একটা heap-এর পুরো lifecycle একসাথে দেখা।

## অনুশীলন

- এই class-টাকেই এবার MaxHeap বানিয়ে ফেলো। ঠিক কোথায় কী বদলাবে, আগে ভেবে নাও।
