---
title: "Stack (LIFO): শেষে ঢোকা আগে বের হয়"
description: থালার স্তূপের মতো stack-এ শেষে রাখাটাই আগে ওঠে। push/pop/peek অপারেশন আর JavaScript array দিয়ে বানানো দেখি।
---

## কী শিখব

- LIFO (Last In, First Out) নীতির stack

## ধারণা

ধরো, ধোয়া থালাগুলো একটার উপর একটা রেখে স্তূপ করছ। এখন থালা লাগলে কোনটা আগে তোলো? সবার উপরেরটা, যেটা তুমি একদম শেষে রেখেছ। এটাই হলো Stack।

এই নীতির নাম LIFO (Last In, First Out): শেষে ঢোকা জিনিস আগে বের হয়। মূল অপারেশন দুটো: `push` (উপরে রাখা), `pop` (উপর থেকে নেওয়া)।

তুমি প্রতিদিনই stack ব্যবহার কর। undo (Ctrl+Z), ব্রাউজারের back button, বইয়ের স্তূপ, সবই এই নিয়মে চলে।

## কোড উদাহরণ

```js
class Stack {
  constructor() { this.items = []; }

  push(x)  { this.items.push(x); }              // উপরে রাখি, O(1)
  pop()    { return this.items.pop(); }         // উপর থেকে নিই, O(1)
  peek()   { return this.items[this.items.length - 1]; } // উপরেরটা দেখি
  isEmpty(){ return this.items.length === 0; }
  size()   { return this.items.length; }
}

const s = new Stack();
s.push(10); s.push(20); s.push(30);
console.log(s.peek()); // 30 (সবার উপরে)
console.log(s.pop());  // 30 (শেষে ঢুকেছিল, আগে বের)
console.log(s.pop());  // 20
```

## কোড ব্রেকডাউন

- JavaScript array-এর `push/pop` কাজ করে array-এর একদম শেষ প্রান্তে, তাই দুটোই O(1)। Stack বানানোর জন্য এর চেয়ে আদর্শ আর কিছু নেই।
- `peek` শুধু উপরেরটা দেখে, বের করে না।

## Complexity

- push/pop/peek, তিনটাই O(1)।

## Animation Spec

> **Animation Spec: Plate Stack**
>
> - **দৃশ্য:** উল্লম্ব স্তূপ; উপরে top pointer।
> - **ইনপুট:** মান; push/pop বাটন।
> - **ধাপ:** push করলে নতুন plate উপরে নামে; pop করলে উপরের plate উঠে যায়। শুধু top-ই নাড়াচাড়া হয়।
> - **লক্ষ্য:** LIFO নীতিটা চোখে দেখা।

## অনুশীলন

- একটা string-এর বন্ধনী `()[]{}` ঠিকঠাক balanced কিনা stack দিয়ে চেক করো।
