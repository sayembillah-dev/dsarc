---
title: "Bellman-Ford Algorithm: Negative Weight সামলানো"
description: সব edge বারবার relax করে shortest path, negative weight আর negative cycle দুটোই সামলায়।
---

## কী শিখব

- negative weight সামলানো shortest path

## ধারণা

Dijkstra-র একটা শর্ত ছিল: weight negative হবে না। কিন্তু negative weight থাকলেই তো আছে, তখন কী করবে? উত্তর **Bellman-Ford**। কৌশলটা অদ্ভুত সহজ: সব edge একটা পুরো রাউন্ডে relax করো, আর এমন রাউন্ড চালাও **V-1 বার**। হ্যাঁ, Dijkstra-র চেয়ে ধীর (O(V * E))। কিন্তু negative weight সামলায়, আর বোনাস হিসেবে **negative cycle**-ও ধরে ফেলে, সেই cycle যেখানে ঘুরতে ঘুরতে খরচ কমতেই থাকে।

## কোড উদাহরণ

```js
// edges: [[u, v, weight], ...]
function bellmanFord(vertices, edges, start) {
  const dist = new Map(vertices.map(v => [v, Infinity]));
  dist.set(start, 0);

  // V-1 বার সব edge relax করি
  for (let i = 0; i < vertices.length - 1; i++) {
    for (const [u, v, w] of edges) {
      if (dist.get(u) + w < dist.get(v)) dist.set(v, dist.get(u) + w);
    }
  }
  // আরেকবার relax হলে, অর্থাৎ negative cycle আছে
  for (const [u, v, w] of edges) {
    if (dist.get(u) + w < dist.get(v)) throw new Error("Negative cycle!");
  }
  return dist;
}
console.log(bellmanFord(
  ["A", "B", "C"],
  [["A", "B", 4], ["A", "C", 5], ["B", "C", -3]],
  "A"
)); // A:0, B:4, C:1
```

## কোড ব্রেকডাউন

- V-1 বার কেন? cycle ছাড়া কোনো shortest path-এ সর্বোচ্চ V-1 টা edge থাকতে পারে। তাই ততবার relax করলেই সব ঠিক হয়ে যায়।
- এখানেই আসল জিনিস: যদি V-তম রাউন্ডেও দূরত্ব কমে, বুঝবে negative cycle আছে। নইলে কমার কথাই নয়।

## Complexity

- O(V * E)।

## Animation Spec

> **Animation Spec: Repeated Relaxation**
>
> - **দৃশ্য:** graph; প্রতিটা round-এ সব edge একে একে relax; distance টেবিল আপডেট হয়।
> - **ইনপুট:** graph (negative weight সহ)।
> - **ধাপ:** round-by-round distance কমছে; শেষ round-এ negative cycle ধরা পড়ল (লাল)।
> - **লক্ষ্য:** Dijkstra-র সাথে trade-off আর negative weight বোঝা।

## অনুশীলন

- Dijkstra আর Bellman-Ford-এর মধ্যে কখন কোনটা নেবে, লিস্ট করে ফেলো।
