---
title: "Map আর Set: বাস্তবে যেটা ব্যবহার করবে"
description: JavaScript-এর built-in Map আর Set-এর দক্ষ ব্যবহার, frequency counting, duplicate সরানো, আর iteration-এর কায়দা।
---

## কী শিখব

- built-in `Map` আর `Set`-এর দক্ষ ব্যবহার

## ধারণা

সত্যি কথা বলতে, বাস্তব কোডে নিজে hash table কেউ বানায় না। JavaScript নিজেই দিয়ে দিয়েছে দুটো দারুণ জিনিস, `Map` আর `Set`।

- **Map**: key-value জোড়া রাখে। key যেকোনো টাইপেরই হতে পারে, আর ঢোকানোর ক্রমটাও বজায় থাকে।
- **Set**: শুধু ইউনিক মান রাখে। কোনো মান আছে কিনা, সেটা চেক করা একদম দ্রুত।

<Callout type="info" title="object না Map?">
object দিয়েও (`{}`) কাজ চালানো যায়। তবে DSA-তে `Map`/`Set` অনেক বেশি নিরাপদ আর দ্রুত, বিশেষ করে ডেটা বেশি হলে।
</Callout>

## কোড উদাহরণ

```js
// Map
const freq = new Map();
for (const ch of "banana") {
  freq.set(ch, (freq.get(ch) || 0) + 1); // অক্ষর গণনা
}
console.log(freq); // Map { 'b'=>1, 'a'=>3, 'n'=>2 }

// Set
const unique = new Set([1, 2, 2, 3, 3, 3]);
console.log([...unique]);      // [1, 2, 3]
console.log(unique.has(2));    // true, O(1)

// iterate
for (const [key, count] of freq) console.log(key, count);
```

## কোড ব্রেকডাউন

- `map.get(k) || 0` মানে কী? মান না থাকলে 0 ধরে নেওয়া। গণনা শুরু করার জন্য এটা খুব চেনা কৌশল।
- `Set` দিয়ে duplicate সরানো যায় মাত্র এক লাইনে: `[...new Set(arr)]`।
- `Map`/`Set`-এর `get`, `set`, `has`, `add`, সবই গড়ে O(1)।

## Complexity

- get/set/has/add গড়ে O(1)।

## Animation Spec

> **Animation Spec: Frequency Counter**
>
> - **দৃশ্য:** উপরে input string, নিচে Map entries (key থেকে count) বার সহ।
> - **ইনপুট:** string।
> - **ধাপ:** প্রতিটা অক্ষরে সংশ্লিষ্ট count-এর বার এক ধাপ বাড়বে।
> - **লক্ষ্য:** frequency counting প্যাটার্নটা আত্মস্থ করা (interview-তে অতি প্রচলিত)।

## অনুশীলন

- একটা string-এ সবচেয়ে বেশি বার আসা অক্ষরটা বের করো।
