---
title: "Singly Linked List: পূর্ণ ক্লাস"
description: head ধরে রেখে prepend, append, find ও print সহ একটা কাজের singly linked list class বানানো।
---

## কী শিখব

- একটা কাজের singly linked list class বানানো

## ধারণা

`head` (প্রথম node) ধরে রাখলে পুরো লিস্টে পৌঁছানো যায়। আমরা `prepend` (শুরুতে), `append` (শেষে), `print`, `find` যোগ করব।

## কোড উদাহরণ

```js
class Node {
  constructor(value) { this.value = value; this.next = null; }
}

class LinkedList {
  constructor() { this.head = null; this.size = 0; }

  // শুরুতে যোগ, O(1)
  prepend(value) {
    const node = new Node(value);
    node.next = this.head;   // নতুন node পুরনো head-কে দেখায়
    this.head = node;        // নতুন node-ই এখন head
    this.size++;
  }

  // শেষে যোগ, O(n) (শেষ node খুঁজতে হয়)
  append(value) {
    const node = new Node(value);
    if (!this.head) { this.head = node; this.size++; return; }
    let current = this.head;
    while (current.next) current = current.next; // শেষে পৌঁছাই
    current.next = node;
    this.size++;
  }

  // মান খোঁজা, O(n)
  find(value) {
    let current = this.head;
    while (current) {
      if (current.value === value) return current;
      current = current.next;
    }
    return null;
  }

  print() {
    const parts = [];
    let current = this.head;
    while (current) { parts.push(current.value); current = current.next; }
    console.log(parts.join(" -> ") + " -> null");
  }
}

const list = new LinkedList();
list.append(10); list.append(20); list.prepend(5);
list.print();               // 5 -> 10 -> 20 -> null
console.log(list.find(20)); // Node { value: 20, ... }
console.log(list.size);     // 3
```

## কোড ব্রেকডাউন

- `prepend`: নতুন node-কে head বানায়, পুরনো head-কে তার next বানায়। কাউকে সরাতে হয় না, তাই O(1)।
- `append`: শেষ node খুঁজতে পুরো লিস্ট হাঁটতে হয়, তাই O(n)।
- traversal প্যাটার্ন মনে রাখো: `current = head; while (current) current = current.next;`

## Complexity

- prepend O(1); append, find, print O(n); space O(n)।

## Animation Spec

> **Animation Spec: Prepend vs Append**
>
> - **দৃশ্য:** node শৃঙ্খল, head লেবেল।
> - **ইনপুট:** মান; prepend/append নির্বাচন।
> - **ধাপ:** prepend করলে নতুন node সামনে বসে head তীর ঘুরে যায় (এক ধাপ); append করলে pointer শেষ পর্যন্ত হেঁটে তারপর জোড়া লাগে (n ধাপ)।
> - **লক্ষ্য:** কেন prepend O(1) আর append O(n)।

## অনুশীলন

- `insertAt(index, value)` মেথড যোগ করো।
