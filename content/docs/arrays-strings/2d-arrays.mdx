---
title: "2D Arrays: Matrix আর Grid"
description: Array-এর ভেতরে array, মানে সারি-কলামের গ্রিড, যেটা image, game, graph সব জায়গায় লাগে।
---

## কী শিখব

- সারি-কলামের গ্রিড নিয়ে কাজ করা

## ধারণা

ধরো তুমি একটা বাসের সিট ম্যাপ বানাচ্ছ। সারি আছে, কলাম আছে। "৫ নম্বর সারির ২ নম্বর সিট" বললেই সবাই বুঝে যাবে ঠিক কোন জায়গার কথা বলছ। 2D array ঠিক এমনই, array-এর ভেতরে array, একটা পুরো টেবিল বা গ্রিড। আর অ্যাক্সেস? `matrix[row][col]` দিয়েই।

## কোড উদাহরণ

```js
// 3×3 গ্রিড
const grid = [
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9],
];

console.log(grid[1][2]); // 6 (২য় সারি, ৩য় কলাম)

// প্রতিটা ঘরে ঘোরা
for (let r = 0; r < grid.length; r++) {
  let line = "";
  for (let c = 0; c < grid[r].length; c++) {
    line += grid[r][c] + " ";
  }
  console.log(line);
}

// খালি m×n গ্রিড বানানো (সব 0)
const rows = 2, cols = 3;
const empty = Array.from({ length: rows }, () => Array(cols).fill(0));
console.log(empty); // [ [0, 0, 0], [0, 0, 0] ]
```

## কোড ব্রেকডাউন

- `grid[row][col]` মানে আগে সারি, তারপর কলাম। ক্রমটা মনে রাখো, উল্টে গেলে ঝামেলা।
- নেস্টেড loop মানে m×n ঘর ঘুরে আসছ, তাই **O(m·n)**।
- আর খালি গ্রিড বানাতে `Array.from({ length }, () => Array(cols).fill(0))`-ই নিরাপদ রাস্তা। এতে প্রতিটা সারি আলাদা array হয়, সব সারিতে একই array-এর reference বসে যাওয়ার ভুলটা এড়ে যাও।

## Complexity

- পুরো গ্রিড ঘুরলে Time: O(m·n)।

## Animation Spec

> **Animation Spec: Grid Walker**
>
> - **দৃশ্য:** m×n রঙিন গ্রিড; একটা কমলা highlight ঘর ঘরে সরবে।
> - **ইনপুট:** rows, cols; traversal order (row-wise / column-wise)।
> - **ধাপ:** নির্বাচিত ক্রমে ঘর highlight হবে, visited ঘর হালকা রঙে পড়ে থাকবে।
> - **লক্ষ্য:** 2D indexing আর O(m·n) বোঝা; পরে island/graph সমস্যায় কাজে দেবে।

## অনুশীলন

- চলো দেখি: একটা 3×3 grid-এর মূল কর্ণের (diagonal) উপাদানগুলোর যোগফল বের করতে পারো কি?
