---
title: "Array Operations: কোন অপারেশনে খরচ কত"
description: Array-এ access, insert, delete, search, প্রতিটার খরচ কত আর কেন, চলো এক নজরে দেখে নিই।
---

## কী শিখব

- insert, delete, search-এর খরচ

## ধারণা

ব্যাপারটা হলো, সব অপারেশনের খরচ সমান না। খরচ নির্ভর করে তুমি array-এর কোথায় হাত দিচ্ছ, তার উপর। শেষে কিছু করলে প্রায় ফ্রি, কিন্তু শুরুতে বা মাঝে হাত দিলেই বাকি সবাইকে সরাতে হয়, আর সেই সরানোটাই আসল খরচ:

| অপারেশন | Complexity | কেন |
|---------|-----------|-----|
| Access `arr[i]` | O(1) | সরাসরি |
| Update `arr[i]=x` | O(1) | সরাসরি |
| Push (শেষে যোগ) | O(1) | কাউকে সরাতে হয় না |
| Pop (শেষে বাদ) | O(1) | কাউকে সরাতে হয় না |
| Unshift (শুরুতে যোগ) | O(n) | সবাইকে ডানে সরাতে হয় |
| Shift (শুরুতে বাদ) | O(n) | সবাইকে বামে সরাতে হয় |
| Search (মান খোঁজা) | O(n) | একটা একটা দেখতে হয় |
| Insert মাঝে | O(n) | পরের সবাইকে সরাতে হয় |

## কোড উদাহরণ

```js
const arr = [10, 20, 30];

arr.push(40);       // [10,20,30,40], O(1)
arr.pop();          // [10,20,30], O(1)
arr.unshift(5);     // [5,10,20,30], O(n)
arr.shift();        // [10,20,30], O(n)

// মাঝে insert (index 1-এ 15 বসাই)
arr.splice(1, 0, 15); // [10,15,20,30], O(n)

// খোঁজা
console.log(arr.indexOf(20));  // 2, O(n)
console.log(arr.includes(99)); // false, O(n)
console.log(arr);              // [10, 15, 20, 30]
```

## কোড ব্রেকডাউন

- শুরুতে বা মাঝে কিছু করলেই বাকিদের **সরাতে** হয়, তাই O(n)।
- শেষে করলে কাউকে সরাতে হয় না, তাই O(1)।
- আর `splice(start, deleteCount, ...items)`? ওটা বহুমুখী, চাইলে বাদও দেয়, চাইলে যোগও করে।

## Animation Spec

> **Animation Spec: Insert/Delete Shifting**
>
> - **দৃশ্য:** array box-এর সারি; একটা "insert at index" নিয়ন্ত্রণ।
> - **ইনপুট:** index আর মান; তারপর অপারেশন নির্বাচন।
> - **ধাপ:** মাঝে insert করলে ডান পাশের সব box এক ঘর করে ডানে স্লাইড করবে (এই স্লাইডটাই O(n)); শেষে insert করলে কেউ নড়বেই না।
> - **লক্ষ্য:** "সরানোর খরচ"-ই complexity-র উৎস, এটা চোখে দেখা।

## অনুশীলন

- ভাবো তো, `push` কেন O(1) আর `unshift` কেন O(n)? নিজের ভাষায়, বন্ধুকে বোঝানোর ঢঙে ব্যাখ্যা করো।
